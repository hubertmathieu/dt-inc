from timeit import default_timer as timer

initialize()

while True:
    # checker la vitesse d'une loop normale
    # regarder comment le freq je peux gere sa
    # implementer une state
    
    # Detect le range
    # Current speed
    # Current angle (sum of all angle )
    # Detect la position en X et Y
    # on a la vitesse donc on integre, on a l'angle
    end = timer()
pass
